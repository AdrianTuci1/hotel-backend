const dateExtractor = require('./dateExtractor');
const productExtractor = require('./productExtractor');
const roomExtractor = require('./roomExtractor');
const contactExtractor = require('./contactExtractor');
const intentExtractor = require('./intentExtractor');

module.exports = {
  dateExtractor,
  productExtractor,
  roomExtractor,
  contactExtractor,
  intentExtractor
}; 